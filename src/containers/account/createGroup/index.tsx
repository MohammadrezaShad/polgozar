import React from 'react';
import styled from 'styled-components';
// import { useGetMyAccountQuery } from 'graphql/types';
import { StepBar } from 'components/elements';

const CreateGroup = () => {
  return (
    <Wrapper>
      <StepBar />
    </Wrapper>
  );
};

export default CreateGroup;

const Wrapper = styled.div``;
